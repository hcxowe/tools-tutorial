// 表单控制绑定
var Vue = require('vue');

var app = new Vue({
    el: '#app',
    data: {
        message: '',
        checked: true,
        checkedNames: [],
        picked: '',
        selected: ''
    }
});