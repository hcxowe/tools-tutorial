/* require.config({
    paths: {
        'jquery': '../jquery-easyui-1.5.2/jquery.min',
        'easyui': '../jquery-easyui-1.5.2/jquery.easyui.min'
    },
    shim: {
        'easyui': {
            // jquery插件，依赖jquery，一旦加载完成，插件附在了jquery全局对象上，可以不使用exprots作为模块的导出值
            deps: ['jquery']
            //exports: 'jQuery.fn.zTree'
        }
    },
    waitSeconds: 15
}); */

require(['./js/a'], function() {
    
});