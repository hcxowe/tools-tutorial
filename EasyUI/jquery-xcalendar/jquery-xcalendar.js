;(function($, window, undefined){
    var pluginName = 'xCalendar',
        defaults = {

        }

    var XCalendar = function(el, opts) {
        this.$el = el
        this.options = $.extend({}, defaults, opts)
    }

    XCalendar.prototype = {

    }

    $.fn.xCalendar = function(opts) {
        
    }
}(jQuery, window))